apiVersion: v1
kind: Secret
metadata:
  name: acm-kessel-spicedb-config
  namespace: acm-kessel
type: Opaque
stringData:
  # IMPORTANT: Replace this with a randomly generated 32-character preshared key
  # You can generate one with: openssl rand -base64 32
  preshared_key: "CHANGE_ME_REPLACE_WITH_RANDOM_KEY"
