apiVersion: v1
kind: Secret
metadata:
  name: acm-kessel-rbac-config
  namespace: acm-kessel
  labels:
    app.kubernetes.io/name: rbac
    app.kubernetes.io/component: api
    app.kubernetes.io/part-of: acm-rbac
type: Opaque
stringData:
  # Django configuration
  django-secret-key: "CHANGE_ME_GENERATE_WITH_openssl_rand_base64_32"

  # Database credentials - will be populated from postgres secret
  # These are placeholders - we'll use secretKeyRef in deployment

  # Optional: Kessel API credentials (if using authentication)
  # relations-api-client-id: ""
  # relations-api-client-secret: ""
  # inventory-api-client-id: ""
  # inventory-api-client-secret: ""
